.DS_Store
.python-version
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Downloaded model assets and local runtime records.
models/*.task
# Keep new runtime data local; explicitly published text snapshot only.
sessions/*
!sessions/archive-20260914/

# Third-party avatar binaries are fetched locally and retain their own licenses.
assets/avatars/live2d/haru/
assets/avatars/vrm/seed-san/

# Local phone/camera bench inputs (large binaries).
benchmarks/inputs/
